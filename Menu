-- Kiểm tra chống spam: Nếu menu hoặc nút nổi đã tồn tại thì dừng luôn
if game.CoreGui:FindFirstChild("TienPhatMenuGui") or game.CoreGui:FindFirstChild("TienPhatToggleGui") then
    return
end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

-- 1. TẠO GIAO DIỆN MENU CHÍNH
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TienPhatMenuGui"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -110)
MainFrame.Size = UDim2.new(0, 280, 0, 220)
MainFrame.Active = true
MainFrame.Draggable = true

-- Tiêu đề
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "  ! Tiến Phát Menu"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 15
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Nút ẩn (-)
local MinBtn = Instance.new("TextButton")
MinBtn.Parent = MainFrame
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
MinBtn.Position = UDim2.new(1, -55, 0, 3)
MinBtn.Size = UDim2.new(0, 24, 0, 24)
MinBtn.Font = Enum.Font.SourceSansBold
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.TextSize = 16

-- Nút đóng (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = MainFrame
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseBtn.Position = UDim2.new(1, -27, 0, 3)
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14

-- Thanh TabBar
local TabBar = Instance.new("Frame")
TabBar.Parent = MainFrame
TabBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
TabBar.Position = UDim2.new(0, 0, 0, 30)
TabBar.Size = UDim2.new(1, 0, 0, 30)

-- Khung chứa nội dung
local Container = Instance.new("Frame")
Container.Parent = MainFrame
Container.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Container.Position = UDim2.new(0, 0, 0, 60)
Container.Size = UDim2.new(1, 0, 1, -60)

local TabMain = Instance.new("ScrollingFrame")
TabMain.Parent = Container
TabMain.Size = UDim2.new(1, 0, 1, 0)
TabMain.BackgroundTransparency = 1
TabMain.Visible = true
TabMain.CanvasSize = UDim2.new(0, 0, 0, 0)

local TabSettings = Instance.new("ScrollingFrame")
TabSettings.Parent = Container
TabSettings.Size = UDim2.new(1, 0, 1, 0)
TabSettings.BackgroundTransparency = 1
TabSettings.Visible = false
TabSettings.CanvasSize = UDim2.new(0, 0, 0, 0)

local TabVisual = Instance.new("ScrollingFrame")
TabVisual.Parent = Container
TabVisual.Size = UDim2.new(1, 0, 1, 0)
TabVisual.BackgroundTransparency = 1
TabVisual.Visible = false
TabVisual.CanvasSize = UDim2.new(0, 0, 0, 0)

local function SwitchTab(selectedTab)
    TabMain.Visible = (selectedTab == TabMain)
    TabSettings.Visible = (selectedTab == TabSettings)
    TabVisual.Visible = (selectedTab == TabVisual)
end

local BtnMain = Instance.new("TextButton")
BtnMain.Parent = TabBar
BtnMain.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
BtnMain.Size = UDim2.new(0.333, 0, 1, 0)
BtnMain.Font = Enum.Font.SourceSansBold
BtnMain.Text = "Main"
BtnMain.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnMain.TextSize = 13
BtnMain.MouseButton1Click:Connect(function() SwitchTab(TabMain) end)

local BtnSettings = Instance.new("TextButton")
BtnSettings.Parent = TabBar
BtnSettings.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
BtnSettings.Position = UDim2.new(0.333, 0, 0, 0)
BtnSettings.Size = UDim2.new(0.333, 0, 1, 0)
BtnSettings.Font = Enum.Font.SourceSansBold
BtnSettings.Text = "Settings"
BtnSettings.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnSettings.TextSize = 13
BtnSettings.MouseButton1Click:Connect(function() SwitchTab(TabSettings) end)

local BtnVisual = Instance.new("TextButton")
BtnVisual.Parent = TabBar
BtnVisual.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
BtnVisual.Position = UDim2.new(0.666, 0, 0, 0)
BtnVisual.Size = UDim2.new(0.334, 0, 1, 0)
BtnVisual.Font = Enum.Font.SourceSansBold
BtnVisual.Text = "Visual"
BtnVisual.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnVisual.TextSize = 13
BtnVisual.MouseButton1Click:Connect(function() SwitchTab(TabVisual) end)

-- ===== TAB MAIN: TÍNH NĂNG ESP BOX =====
local EspBtn = Instance.new("TextButton")
EspBtn.Parent = TabMain
EspBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
EspBtn.Position = UDim2.new(0.1, 0, 0.1, 0)
EspBtn.Size = UDim2.new(0.8, 0, 0, 35)
EspBtn.Font = Enum.Font.SourceSansBold
EspBtn.Text = "ESP Box: Off"
EspBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
EspBtn.TextSize = 14

local espEnabled = false
local espCache = {}

local function RemoveEsp(player)
    if espCache[player] then
        if espCache[player].box then espCache[player].box:Remove() end
        if espCache[player].name then espCache[player].name:Remove() end
        espCache[player] = nil
    end
end

EspBtn.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    if espEnabled then
        EspBtn.Text = "ESP Box: On"
        EspBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        EspBtn.Text = "ESP Box: Off"
        EspBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        for _, p in pairs(Players:GetPlayers()) do
            RemoveEsp(p)
        end
    end
end)

-- Vòng lặp vẽ ESP chạy mỗi khung hình
RunService.RenderStepped:Connect(function()
    if not espEnabled then return end
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") then
            local character = player.Character
            local rootPart = character.HumanoidRootPart
            local humanoid = character.Humanoid
            
            if humanoid.Health > 0 then
                if not espCache[player] then
                    local box = Drawing.new("Square")
                    box.Visible = false
                    box.Filled = false
                    box.Thickness = 1.5
                    box.Color = Color3.fromRGB(255, 0, 0)
                    
                    local nameTag = Drawing.new("Text")
                    nameTag.Visible = false
                    nameTag.Center = true
                    nameTag.Outline = true
                    nameTag.Font = 2
                    nameTag.Size = 16
                    nameTag.Color = Color3.fromRGB(255, 255, 255)
                    
                    espCache[player] = {box = box, name = nameTag}
                end
                
                local box = espCache[player].box
                local nameTag = espCache[player].name
                
                local vector, onScreen = Camera:WorldToViewportPoint(rootPart.Position)
                
                if onScreen then
                    local head = character:FindFirstChild("Head")
                    local distance = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and (rootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude or 0
                    
                    if head then
                        local headVector = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0))
                        local legVector = Camera:WorldToViewportPoint(rootPart.Position - Vector3.new(0, 3, 0))
                        
                        local height = math.abs(headVector.Y - legVector.Y)
                        local width = height / 2
                        
                        box.Size = Vector2.new(width, height)
                        box.Position = Vector2.new(vector.X - width / 2, headVector.Y)
                        box.Visible = true
                        
                        nameTag.Text = player.Name .. " [" .. math.floor(distance) .."m]"
                        nameTag.Position = Vector2.new(vector.X, headVector.Y - 20)
                        nameTag.Visible = true
                    else
                        box.Visible = false
                        nameTag.Visible = false
                    end
                else
                    box.Visible = false
                    nameTag.Visible = false
                end
            else
                RemoveEsp(player)
            end
        else
            RemoveEsp(player)
        end
    end
end)

Players.PlayerRemoving:Connect(function(player)
    RemoveEsp(player)
end)

-- Nội dung Tab Settings
local TxtSettings = Instance.new("TextLabel")
TxtSettings.Parent = TabSettings
TxtSettings.Size = UDim2.new(1, 0, 1, 0)
TxtSettings.BackgroundTransparency = 1
TxtSettings.Font = Enum.Font.SourceSans
TxtSettings.Text = "Tab Settings (Đang trống)..."
TxtSettings.TextColor3 = Color3.fromRGB(150, 150, 150)
TxtSettings.TextSize = 14

-- ===== TAB VISUAL: TÍNH NĂNG FIX LAG =====
local FixLagBtn = Instance.new("TextButton")
FixLagBtn.Parent = TabVisual
FixLagBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
FixLagBtn.Position = UDim2.new(0.1, 0, 0.1, 0)
FixLagBtn.Size = UDim2.new(0.8, 0, 0, 35)
FixLagBtn.Font = Enum.Font.SourceSansBold
FixLagBtn.Text = "Fix Lag: Off"
FixLagBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FixLagBtn.TextSize = 14

local ModeFrame = Instance.new("Frame")
ModeFrame.Parent = TabVisual
ModeFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ModeFrame.Position = UDim2.new(0.1, 0, 0.32, 0)
ModeFrame.Size = UDim2.new(0.8, 0, 0, 75)
ModeFrame.Visible = false

local ModeTitle = Instance.new("TextLabel")
ModeTitle.Parent = ModeFrame
ModeTitle.Size = UDim2.new(1, 0, 0, 25)
ModeTitle.BackgroundTransparency = 1
ModeTitle.Font = Enum.Font.SourceSansBold
ModeTitle.Text = "Chọn mức độ Fix Lag:"
ModeTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ModeTitle.TextSize = 13

local BtnNhe = Instance.new("TextButton")
BtnNhe.Parent = ModeFrame
BtnNhe.BackgroundColor3 = Color3.fromRGB(0, 120, 200)
BtnNhe.Position = UDim2.new(0.05, 0, 0.4, 0)
BtnNhe.Size = UDim2.new(0.42, 0, 0, 32)
BtnNhe.Font = Enum.Font.SourceSansBold
BtnNhe.Text = "Nhẹ"
BtnNhe.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnNhe.TextSize = 13

local BtnTrungBinh = Instance.new("TextButton")
BtnTrungBinh.Parent = ModeFrame
BtnTrungBinh.BackgroundColor3 = Color3.fromRGB(200, 100, 0)
BtnTrungBinh.Position = UDim2.new(0.53, 0, 0.4, 0)
BtnTrungBinh.Size = UDim2.new(0.42, 0, 0, 32)
BtnTrungBinh.Font = Enum.Font.SourceSansBold
BtnTrungBinh.Text = "Trung Bình"
BtnTrungBinh.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnTrungBinh.TextSize = 13

FixLagBtn.MouseButton1Click:Connect(function()
    ModeFrame.Visible = not ModeFrame.Visible
end)

local function RemoveFog()
    pcall(function()
        Lighting.FogEnd = 999999
        if Lighting:FindFirstChild("Atmosphere") then
            Lighting.Atmosphere.Density = 0
        end
    end)
end

local function RemoveOutfits()
    pcall(function()
        if LocalPlayer.Character then
            for _, v in pairs(LocalPlayer.Character:GetChildren()) do
                if v:IsA("Accessory") or v:IsA("Clothing") or v:IsA("ShirtGraphic") then
                    v:Destroy()
                end
            end
        end
    end)
end

local function RemoveTextures()
    pcall(function()
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Texture") or v:IsA("Decal") then
                v:Destroy()
            elseif v:IsA("BasePart") then
                v.Material = Enum.Material.SmoothPlastic
                v.Reflectance = 0
            end
        end
    end)
end

BtnNhe.MouseButton1Click:Connect(function()
    RemoveFog()
    RemoveOutfits()
    FixLagBtn.Text = "Fix Lag: Nhẹ (On)"
    FixLagBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    ModeFrame.Visible = false
end)

BtnTrungBinh.MouseButton1Click:Connect(function()
    RemoveFog()
    RemoveOutfits()
    RemoveTextures()
    FixLagBtn.Text = "Fix Lag: Trung Bình (On)"
    FixLagBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    ModeFrame.Visible = false
end)

-- 2. TẠO DUY NHẤT 1 NÚT TRÒN NỔI NGOÀI MÀN HÌNH (Chống nhân đôi)
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "TienPhatToggleGui"
ToggleGui.Parent = game.CoreGui

local OpenBtn = Instance.new("TextButton")
OpenBtn.Parent = ToggleGui
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 200)
OpenBtn.Position = UDim2.new(0, 20, 0.5, -20)
OpenBtn.Size = UDim2.new(0, 50, 0, 40)
OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.Text = "XCheat"
OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.TextSize = 13
OpenBtn.Draggable = true

local isOpen = true

local function ToggleMenu(state)
    isOpen = state
    MainFrame.Visible = isOpen
    if isOpen then
        OpenBtn.Text = "On"
        OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        OpenBtn.Text = "Off"
        OpenBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    end
end

OpenBtn.MouseButton1Click:Connect(function()
    ToggleMenu(not isOpen)
end)

MinBtn.MouseButton1Click:Connect(function()
    ToggleMenu(false)
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
    ToggleGui:Destroy()
end)

-- Sau 1 giây đồng bộ chữ XCheat thành On/Off theo trạng thái menu
task.delay(1, function()
    ToggleMenu(isOpen)
end)
