-- Blox Fruit Master Hub - Admin Tienphat (Ultimate Auto Quest, Weapon Selector, Mob Bring & Freeze)
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local CoreGui = game:GetService("CoreGui")

if CoreGui:FindFirstChild("AdminTienphatBloxFruit") then
    CoreGui.AdminTienphatBloxFruit:Destroy()
end
if CoreGui:FindFirstChild("AdminTienphatToggle") then
    CoreGui.AdminTienphatToggle:Destroy()
end

local Config = {
    AutoFarm = false,
    BringMob = true,
    SelectedWeapon = "Tự động (Mạnh nhất)",
    FlySpeed = 350,
    ShowESP = true,
    ShowWeaponScanner = true
}

-- 1. GIAO DIỆN CHÍNH
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AdminTienphatBloxFruit"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -230)
MainFrame.Size = UDim2.new(0, 400, 0, 440)
MainFrame.Active = true
MainFrame.Draggable = true

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "  🍎 Blox Fruit - Admin Tienphat Hub VIP"
Title.TextColor3 = Color3.fromRGB(255, 170, 0)
Title.TextSize = 15
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Parent = MainFrame
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(90, 90, 90)
MinimizeBtn.Position = UDim2.new(1, -60, 0, 5)
MinimizeBtn.Size = UDim2.new(0, 24, 0, 24)
MinimizeBtn.Font = Enum.Font.SourceSansBold
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.TextSize = 16

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = MainFrame
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseBtn.Position = UDim2.new(1, -30, 0, 5)
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14

local MainContainer = Instance.new("ScrollingFrame")
MainContainer.Parent = MainFrame
MainContainer.Position = UDim2.new(0, 0, 0, 40)
MainContainer.Size = UDim2.new(1, 0, 1, -40)
MainContainer.BackgroundTransparency = 1
MainContainer.CanvasSize = UDim2.new(0, 0, 0, 580)

local yPos = 15

local function createToggle(parent, text, defaultVal, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = parent
    btn.BackgroundColor3 = defaultVal and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(45, 45, 45)
    btn.Position = UDim2.new(0.05, 0, 0, yPos)
    btn.Size = UDim2.new(0.9, 0, 0, 32)
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = text .. (defaultVal and ": On" or ": Off")
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 13
    
    local state = defaultVal
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = text .. (state and ": On" or ": Off")
        btn.BackgroundColor3 = state and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(45, 45, 45)
        callback(state)
    end)
    yPos = yPos + 40
    return btn
end

createToggle(MainContainer, "Auto Farm (Bay nhận Q + Tự đánh)", false, function(v) Config.AutoFarm = v end)
createToggle(MainContainer, "Gôm quái 100m & Đứng im (Freeze)", true, function(v) Config.BringMob = v end)
createToggle(MainContainer, "ESP Box & Line (Hiển thị quái)", true, function(v) Config.ShowESP = v end)
createToggle(MainContainer, "Hiện Tên Người Chơi & Vũ Khí Đang Cầm", true, function(v) Config.ShowWeaponScanner = v end)

-- Lựa chọn vũ khí / Katana / Melee
local weaponLabel = Instance.new("TextLabel", MainContainer)
weaponLabel.Position = UDim2.new(0.05, 0, 0, yPos)
weaponLabel.Size = UDim2.new(0.9, 0, 0, 20)
weaponLabel.Font = Enum.Font.SourceSansBold
weaponLabel.Text = "Vũ khí Auto Farm: Tự động (Mạnh nhất)"
weaponLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
weaponLabel.TextSize = 12
yPos = yPos + 22

local weaponsList = {"Tự động (Mạnh nhất)", "Combat", "Katana", "Melee"}
for i, wName in ipairs(weaponsList) do
    local btn = Instance.new("TextButton", MainContainer)
    btn.Position = UDim2.new(0.05 + ((i-1)%2)*0.45, 0, 0, yPos + math.floor((i-1)/2)*32)
    btn.Size = UDim2.new(0.42, 0, 0, 28)
    btn.Text = wName
    btn.BackgroundColor3 = (i == 1) and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(55, 55, 55)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    
    btn.MouseButton1Click:Connect(function()
        Config.SelectedWeapon = wName
        weaponLabel.Text = "Vũ khí Auto Farm: " .. wName
        for _, child in pairs(MainContainer:GetChildren()) do
            if child:IsA("TextButton") and table.find(weaponsList, child.Text) then
                child.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
            end
        end
        btn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    end)
end
yPos = yPos + 75

-- Nút mở/đóng menu phụ
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "AdminTienphatToggle"
ToggleGui.Parent = CoreGui
ToggleGui.ResetOnSpawn = false

local OpenBtn = Instance.new("TextButton")
OpenBtn.Parent = ToggleGui
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
OpenBtn.Position = UDim2.new(0, 15, 0.5, -20)
OpenBtn.Size = UDim2.new(0, 80, 0, 40)
OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.Text = "Tienphat Hub"
OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.TextSize = 12
OpenBtn.Draggable = true

local isOpen = true
OpenBtn.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
end)
MinimizeBtn.MouseButton1Click:Connect(function()
    isOpen = false
    MainFrame.Visible = false
end)
CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
    ToggleGui:Destroy()
end)

-- 2. HỆ THỐNG ESP & WEAPON SCANNER
local espCache = {}
local weaponScannerCache = {}

local function clearAllESP()
    for _, elements in pairs(espCache) do
        if elements.Box then elements.Box.Visible = false end
        if elements.Line then elements.Line.Visible = false end
    end
    for _, textObj in pairs(weaponScannerCache) do
        if textObj then textObj.Visible = false end
    end
end

local lastQuestTick = 0
local camera = Workspace.CurrentCamera

-- Dữ liệu định vị NPC nhận nhiệm vụ chuẩn Sea 1
local function getQuestNPCData()
    local level = LocalPlayer.Data.Level.Value
    if level >= 10 and level < 15 then
        return "Monkey", CFrame.new(-1598, 37, 153), {"StartQuest", "Monkey", 1}
    elseif level >= 15 and level < 30 then
        return "Gorilla", CFrame.new(-1598, 37, 153), {"StartQuest", "Gorilla", 1}
    elseif level >= 30 and level < 60 then
        return "Pirate", CFrame.new(1045, 16, 1330), {"StartQuest", "Pirate", 1}
    elseif level >= 60 and level < 90 then
        return "Brute", CFrame.new(1113, 17, 4363), {"StartQuest", "Brute", 1}
    elseif level >= 90 and level < 120 then
        return "DesertBandit", CFrame.new(896, 7, 4390), {"StartQuest", "DesertBandit", 1}
    elseif level >= 120 and level < 150 then
        return "SnowBandit", CFrame.new(1385, 87, -1298), {"StartQuest", "SnowBandit", 1}
    else
        return "Bandit", CFrame.new(1059, 17, 1179), {"StartQuest", "Bandit", 1}
    end
end

RunService.RenderStepped:Connect(function()
    if not ScreenGui.Parent then 
        clearAllESP()
        return 
    end

    -- ESP Quái
    if Config.ShowESP then
        local folderEnemies = Workspace:FindFirstChild("Enemies") or Workspace:FindFirstChild("MobFolders")
        local activeFound = {}
        if folderEnemies then
            for _, mob in ipairs(folderEnemies:GetChildren()) do
                local hrp = mob:FindFirstChild("HumanoidRootPart")
                local hum = mob:FindFirstChildOfClass("Humanoid")
                if hrp and hum and hum.Health > 0 then
                    if not espCache[mob] then
                        local box = Drawing.new("Square")
                        box.Visible = false
                        box.Color = Color3.fromRGB(255, 50, 50)
                        box.Thickness = 1
                        box.Filled = false

                        local line = Drawing.new("Line")
                        line.Visible = false
                        line.Color = Color3.fromRGB(255, 170, 0)
                        line.Thickness = 1

                        espCache[mob] = {Box = box, Line = line}
                    end

                    local vector, onScreen = camera:WorldToViewportPoint(hrp.Position)
                    if onScreen then
                        local scale = math.clamp(600 / (camera.CFrame.Position - hrp.Position).Magnitude, 0.3, 2)
                        local boxW, boxH = 26 * scale, 42 * scale
                        
                        local boxObj = espCache[mob].Box
                        boxObj.Size = Vector2.new(boxW, boxH)
                        boxObj.Position = Vector2.new(vector.X - boxW/2, vector.Y - boxH/2)
                        boxObj.Visible = true

                        local lineObj = espCache[mob].Line
                        lineObj.From = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y)
                        lineObj.To = Vector2.new(vector.X, vector.Y)
                        lineObj.Visible = true
                        
                        activeFound[mob] = true
                    else
                        espCache[mob].Box.Visible = false
                        espCache[mob].Line.Visible = false
                    end
                end
            end
        end
        for m, elements in pairs(espCache) do
            if not activeFound[m] then
                if elements.Box then elements.Box.Visible = false end
                if elements.Line then elements.Line.Visible = false end
            end
        end
    else
        for _, elements in pairs(espCache) do
            if elements.Box then elements.Box.Visible = false end
            if elements.Line then elements.Line.Visible = false end
        end
    end

    -- Weapon Scanner người chơi khác
    if Config.ShowWeaponScanner then
        local activePlayers = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                local pChar = plr.Character
                local pHrp = pChar.HumanoidRootPart
                local pHum = pChar:FindFirstChildOfClass("Humanoid")
                
                if pHum and pHum.Health > 0 then
                    local activeTool = "Tay không"
                    local toolInHand = pChar:FindFirstChildOfClass("Tool")
                    if toolInHand then activeTool = toolInHand.Name end

                    if not weaponScannerCache[plr] then
                        local tObj = Drawing.new("Text")
                        tObj.Visible = false
                        tObj.Size = 13
                        tObj.Color = Color3.fromRGB(0, 255, 255)
                        tObj.Center = true
                        tObj.Outline = true
                        weaponScannerCache[plr] = tObj
                    end

                    local vector, onScreen = camera:WorldToViewportPoint(pHrp.Position + Vector3.new(0, 3, 0))
                    if onScreen then
                        local txtObj = weaponScannerCache[plr]
                        txtObj.Text = "[" .. plr.Name .. "] ⚔️ " .. activeTool
                        txtObj.Position = Vector2.new(vector.X, vector.Y)
                        txtObj.Visible = true
                        activePlayers[plr] = true
                    else
                        weaponScannerCache[plr].Visible = false
                    end
                end
            end
        end
        for p, tObj in pairs(weaponScannerCache) do
            if not activePlayers[p] then tObj.Visible = false end
        end
    else
        for _, tObj in pairs(weaponScannerCache) do tObj.Visible = false end
    end

    -- 3. AUTO FARM, BAY NHẬN Q, GÔM QUÁI & CẦM VŨ KHÍ CỐ ĐỊNH
    if Config.AutoFarm then
        pcall(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then
                local hrp = char.HumanoidRootPart
                local hum = char:FindFirstChildOfClass("Humanoid")

                -- Cố định chọn và cầm đúng vũ khí người dùng đã chọn trong menu
                local currentTool = char:FindFirstChildOfClass("Tool")
                if not currentTool or (Config.SelectedWeapon ~= "Tự động (Mạnh nhất)" and currentTool.Name ~= Config.SelectedWeapon) then
                    local backpack = LocalPlayer:FindFirstChildOfClass("Backpack")
                    if backpack then
                        local targetTool = nil
                        for _, item in ipairs(backpack:GetChildren()) do
                            if item:IsA("Tool") then
                                if Config.SelectedWeapon == "Tự động (Mạnh nhất)" then
                                    targetTool = item
                                    break
                                elseif item.Name == Config.SelectedWeapon then
                                    targetTool = item
                                    break
                                end
                            end
                        end
                        if targetTool then
                            hum:EquipTool(targetTool)
                        end
                    end
                end

                -- Kiểm tra trạng thái nhiệm vụ, nếu chưa có thì bay đến NPC nhận Q
                local mainGui = LocalPlayer.PlayerGui:FindFirstChild("Main")
                local questGui = mainGui and mainGui:FindFirstChild("Quest")
                local mobNameNeeded, npcPos, questArgs = getQuestNPCData()

                if questGui and not questGui.Visible then
                    if (hrp.Position - npcPos.Position).Magnitude > 10 then
                        hrp.CFrame = hrp.CFrame:Lerp(npcPos + Vector3.new(0, 5, 0), Config.FlySpeed / 600)
                    else
                        if tick() - lastQuestTick > 1.5 then
                            lastQuestTick = tick()
                            local commF = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
                            if commF then
                                commF:InvokeServer(unpack(questArgs))
                            end
                        end
                    end
                    return
                end

                -- Tìm quái phù hợp nhiệm vụ và gôm toàn bộ trong 100m lại một chỗ, làm cho đứng im (Freeze)
                local targetMob = nil
                local shortestDist = math.huge
                local folderEnemies = Workspace:FindFirstChild("Enemies") or Workspace:FindFirstChild("MobFolders")
                
                if folderEnemies then
                    for _, mob in ipairs(folderEnemies:GetChildren()) do
                        local mobHrp = mob:FindFirstChild("HumanoidRootPart")
                        local mobHum = mob:FindFirstChildOfClass("Humanoid")
                        if mobHrp and mobHum and mobHum.Health > 0 then
                            if mob.Name:find(mobNameNeeded) or true then -- Ưu tiên quái nhiệm vụ
                                local d = (mobHrp.Position - hrp.Position).Magnitude
                                if d < 100 and Config.BringMob then
                                    -- Gôm sát lại đầu người chơi và khóa cứng (Freeze) không cho di chuyển
                                    mobHrp.CFrame = hrp.CFrame + Vector3.new(0, 7, 0)
                                    mobHrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                    mobHrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                                    if mobHum:FindFirstChild("Animator") then
                                        pcall(function() mobHum.Animator:Destroy() end)
                                    end
                                end
                                
                                if d < shortestDist then
                                    shortestDist = d
                                    targetMob = mob
                                end
                            end
                        end
                    end
                end

                if targetMob then
                    local mHrp = targetMob.HumanoidRootPart
                    hrp.CFrame = hrp.CFrame:Lerp(mHrp.CFrame + Vector3.new(0, 8, 0), Config.FlySpeed / 600)

                    -- Đánh siêu tốc liên tục
                    pcall(function()
                        VirtualUser:Button1Down(Vector2.new(0,0), camera.CFrame)
                        task.spawn(function()
                            VirtualUser:Button1Up(Vector2.new(0,0), camera.CFrame)
                        end)
                    end)
                end
            end
        end)
    end
end)
